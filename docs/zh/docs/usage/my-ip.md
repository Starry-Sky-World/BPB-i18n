# :material-fingerprint:{ .md .middle } 我的 IP

![我的 IP](../images/my-ip.jpg)

连接代理后刷新此区域查看 IP。表格显示两类目标：

- 第一行显示 Cloudflare 目标地址的节点 IP。如果设置了 Proxy IP，这里应显示该 IP；而其他目标会使用随机 Cloudflare IP，可用于验证 Proxy IP 是否生效。
- 使用 Warp 配置时，两行应显示相同 IP。

!!! warning
    请关闭 `uBlock`、`AdGuard` 等扩展，否则该区域可能无法正常工作。
